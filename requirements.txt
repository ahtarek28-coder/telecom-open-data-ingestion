# Mirrors [project.dependencies] in pyproject.toml -- kept in sync manually.
# Use this if you just want `pip install -r requirements.txt` instead of
# installing odingest as a package (`pip install -e .`).
requests>=2.31
click>=8.1
duckdb>=1.0
